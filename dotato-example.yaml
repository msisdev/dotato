version: v1

mode: file

plans:
  all:
  default:  []
  fedora:   [kde, konsole, zsh]
  hyprland: [alacritty, fish, waybar]

groups:
  alacritty:
    nux: "~/.config/alacritty"

  bash:
    nux: "~"

  fish:
    nux: "~/.config/fish"

  kde:
    nux: "~"

  konsole:
    nux: "~/.config"

  waybar:
    nux: "~/.config/waybar"
    
  zsh:
    nux: "~"
